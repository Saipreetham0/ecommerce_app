import 'package:flutter/material.dart';

class WhistlistScreen extends StatelessWidget {
  const WhistlistScreen({super.key});

  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Whistlist'),
      ),
      body: const Center(
        child: Text('Whistlist'),
      ),
    );
  }
}
